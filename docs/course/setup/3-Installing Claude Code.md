1. Get a Claude Pro account at claude.ai
2. Make sure you installed npm and that is working
3. In the terminal(Mac) or in Powershell (Win) run the following command:
```
npm install -g @anthropic-ai/claude-code
```